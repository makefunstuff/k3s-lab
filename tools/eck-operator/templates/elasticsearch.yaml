apiVersion: elasticsearch.k8s.elastic.co/v1
kind: Elasticsearch
metadata:
  name: log-storage
spec:
  version: "7.1.17"
  nodeSets:
  - name: default
    count: 1
    config:
      node.store.allow_mmap: false
